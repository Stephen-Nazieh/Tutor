FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install --omit=dev
COPY tsconfig.json ./
COPY src ./src
RUN npm run build
# Cloud Run sets PORT env var, app should use it
ENV PORT=8080
EXPOSE 8080
CMD ["node", "dist/server/index.js"]
