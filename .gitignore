services/adk/node_modules/
node_modules/
.next/

# Environment files (never commit secrets)
.env
.env.*
!.env.example
!.env.*.example

# Office documents
*.docx
*.doc
~$*

# GCP service-account keys (never commit secrets)
forclaude.json
*-sa-key.json
*service-account*.json
gcp-*.json

# Database backups (contain user data)
_backups/
